# AI-Recommendation-Logic
This project implements a content-based recommendation system that suggests suitable career paths based on user skills. It uses TF-IDF vectorization and Cosine Similarity to compare user profiles with job roles. The system ranks jobs by relevance and displays the top recommendations with match percentages.
